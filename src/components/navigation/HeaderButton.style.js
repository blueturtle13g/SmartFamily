import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
    height: '100%',
    justifyContent: 'center',
    minWidth: 80,
  },
});
